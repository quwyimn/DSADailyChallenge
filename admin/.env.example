# Copy to .env for local dev, or set VITE_API_URL in Vercel environment variables for production

# Development (local backend)
VITE_API_URL=http://localhost:3000/api

# Production (set this in Vercel project settings — do NOT commit the real value here)
# VITE_API_URL=https://your-render-backend.onrender.com/api
